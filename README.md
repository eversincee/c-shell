# C-Shell

This is a simple Unix-like shell written in C made as a learning experience

## Usage

1. Compile the program:

```bash
gcc main.c -o shell
```

2. Run the shell:
```bash
./shell
```

To exit the shell:
```bash
> exit
```